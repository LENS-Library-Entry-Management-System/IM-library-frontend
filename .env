
# Frontend environment variables
# API base URL used by the frontend to call the backend API.
# Change this to your backend address when not running locally.
VITE_API_BASE=https://b7593f8d2b9d.ngrok-free.app

# Optional: application name shown in UI (used by some components)
VITE_APP_NAME=im-library-frontend

